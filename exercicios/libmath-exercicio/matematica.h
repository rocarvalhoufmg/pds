/**
 * Calcula o seno do ângulo fornecido em radianos.
 * 
 * @param angulo_radianos O ângulo em radianos para o qual calcular o seno.
 * @return float O seno do ângulo fornecido.
 */
float calcular_seno(float angulo_radianos);

/**
 * Calcula o cosseno do ângulo fornecido em radianos.
 * 
 * @param angulo_radianos O ângulo em radianos para o qual calcular o cosseno.
 * @return float O cosseno do ângulo fornecido.
 */
float calcular_cosseno(float angulo_radianos);

/**
 * Calcula a tangente do ângulo fornecido em radianos.
 * 
 * @param angulo_radianos O ângulo em radianos para o qual calcular a tangente.
 * @return float A tangente do ângulo fornecido.
 */
float calcular_tangente(float angulo_radianos);